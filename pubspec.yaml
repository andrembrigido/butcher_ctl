name: butcher_ctl
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter

  # Ícones de animais (ex: FontAwesomeIcons.dog)
  font_awesome_flutter: ^10.7.0

  # Ícones estilo iOS
  cupertino_icons: ^1.0.8

  # 📅 Pacote para formatação de datas
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
